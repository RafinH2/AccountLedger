apiVersion: v1
kind: Service
metadata:
  name: account-ledger-api
  namespace: account-ledger-dev
spec:
  selector:
    app: account-ledger-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
